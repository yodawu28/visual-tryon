# Visual Fitting Room UI

React + Tailwind source for the kiosk fitting-room application.

## Commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

`npm run build` writes the production static bundle to `../kiosk-demo`, which is the directory served by FastAPI at `/kiosk/`.
